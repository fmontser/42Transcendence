{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Attach to Docker Node App",
            "address": "localhost",
            "port": 9229,

             "remoteRoot": "/usr/src/app/dist",
             "localRoot": "${workspaceFolder}/backEnd/debugger/src/",


			"resolveSourceMapLocations": ["${workspaceFolder}/**", "!**/node_modules/**"],
            "trace": true // Mantén esto
        }
    ]
}